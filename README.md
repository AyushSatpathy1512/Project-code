Build LWC for both Opportunity and Case record pages: Shipment status card with visual progress bar (Ordered → Picked → Shipped → In Transit → Delivered), tracking number with external link, ETA countdown, 'Refresh Status' button (calls API), and error display via Nebula Logger. Component should handle API loading states gracefully.
